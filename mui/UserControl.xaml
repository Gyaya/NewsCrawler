<UserControl x:Class="mui.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Button Content="Button" HorizontalAlignment="Left" Margin="638,36,0,0" VerticalAlignment="Top" Width="105" Height="35" Click="Button_Click"/>
        <ProgressBar Name="pb" HorizontalAlignment="Left" Height="35" Margin="26,36,0,0" VerticalAlignment="Top" Width="536"/>
        <ListView HorizontalAlignment="Left" Height="273" Margin="26,122,0,0" VerticalAlignment="Top" Width="717">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
